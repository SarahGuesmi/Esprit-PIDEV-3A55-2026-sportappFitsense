import { startStimulusApp } from '@symfony/stimulus-bundle';

const app = startStimulusApp();
// register any custom, external controllers here
// app.register('some_controller_name', SomeImportedController);
