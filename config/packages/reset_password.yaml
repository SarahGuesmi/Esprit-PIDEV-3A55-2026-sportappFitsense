symfonycasts_reset_password:
    request_password_repository: App\Repository\ResetPasswordRequestRepository
    # Throttle time in seconds (default is 3600 = 1 hour)
    # Set to 60 seconds (1 minute) for development
    throttle_limit: 60
