<aside class="sidebar fixed inset-y-0 left-0 z-40 w-64 transform transition-transform duration-300 lg:translate-x-0 -translate-x-full">
    <div class="h-full flex flex-col">
        <!-- FitSense Header with Logo -->
        <div class="p-6 pb-4 border-b border-gray-700">
            <div class="text-center">
                <h1 class="text-2xl font-bold mb-3">
                    <span class="bg-gradient-to-r from-blue-400 to-purple-400 bg-clip-text text-transparent">
                        FitSense
                    </span>
                </h1>
                <img src="/assets/images/sport-hero.png" alt="FitSense" class="mx-auto w-28 mb-2 animate-float">
                <p class="text-gray-400 text-xs font-medium uppercase tracking-wide">User Portal</p>
            </div>
        </div>

        <nav class="flex-1 px-3 py-6 space-y-1 overflow-y-auto">
            <a href="{{ path('dashboard_user')|default('#') }}" class="sidebar-link {{ app.request.attributes.get('_route') == 'dashboard_user' ? 'active' : '' }}">
                <i class="fas fa-tachometer-alt w-5 mr-3"></i>
                Dashboard
            </a>

            <a href="#" class="sidebar-link {{ app.request.attributes.get('_route') == 'workouts' ? 'active' : '' }}">
                <i class="fas fa-dumbbell w-5 mr-3"></i>
                Workouts
            </a>

            <a href="{{ path('etat_mental_new') }}" class="sidebar-link {{ app.request.attributes.get('_route') starts with 'etat_mental' ? 'active' : '' }}">
                <i class="fas fa-brain w-5 mr-3"></i>
                Mental Health
            </a>

            <a href="#" class="sidebar-link {{ app.request.attributes.get('_route') == 'nutrition' ? 'active' : '' }}">
                <i class="fas fa-utensils w-5 mr-3"></i>
                Nutrition
            </a>
        </nav>

        <!-- Profile Section -->
        <div class="px-3 py-6 border-t border-gray-700">
            <p class="px-3 text-xs font-semibold text-gray-400 uppercase tracking-wide mb-3">Settings</p>
            <a href="{{ path('profile_setup_height') }}" class="sidebar-link">
                <i class="fas fa-user-cog w-5 mr-3"></i>
                Profile Setup
            </a>
        </div>

        <!-- Logout -->
        <div class="p-3 border-t border-gray-700 mt-auto">
            <a href="#" class="sidebar-link logout-link hover:bg-red-500/10 hover:text-red-400">
                <i class="fas fa-sign-out-alt w-5 mr-3"></i>
                Logout
            </a>
        </div>
    </div>
</aside>

<style>
    @keyframes float {
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-10px); }
    }
    
    .animate-float {
        animation: float 3s ease-in-out infinite;
    }
</style>
