<aside class="sidebar fixed inset-y-0 left-0 z-40 w-64 transform transition-transform duration-300 lg:translate-x-0 -translate-x-full">
    <div class="h-full flex flex-col">
        <!-- FitSense Header with Logo -->
        <div class="p-6 pb-4 border-b border-gray-700">
            <div class="text-center">
                <h1 class="text-2xl font-bold mb-3">
                    <span class="bg-gradient-to-r from-blue-400 to-purple-400 bg-clip-text text-transparent">
                        FitSense
                    </span>
                </h1>
                <img src="/assets/images/sport-hero.png" alt="FitSense" class="mx-auto w-28 mb-2 animate-float">
                <p class="text-gray-400 text-xs font-medium uppercase tracking-wide">Coach Panel</p>
            </div>
        </div>

        <!-- Navigation principale -->
        <nav class="flex-1 px-3 py-6 space-y-1 overflow-y-auto">
            <a href="{{ path('coach_dashboard') }}" class="sidebar-link {{ app.request.attributes.get('_route') == 'coach_dashboard' ? 'active' : '' }}">
                <i class="fas fa-tachometer-alt w-5 mr-3"></i>
                Dashboard
            </a>

            <a href="{{ path('coach_users_index') }}" class="sidebar-link {{ app.request.attributes.get('_route') == 'coach_users_index' ? 'active' : '' }}">
                <i class="fas fa-users w-5 mr-3"></i>
                Users
            </a>

            <a href="{{ path('coach_workout_catalog') }}" class="sidebar-link {{ app.request.attributes.get('_route') == 'coach_workout_catalog' ? 'active' : '' }}">
                <i class="fas fa-dumbbell w-5 mr-3"></i>
                Workouts
            </a>

            <a href="{{ path('coach_questionnaires') }}" class="sidebar-link {{ app.request.attributes.get('_route') == 'coach_questionnaires' ? 'active' : '' }}">
                <i class="fas fa-question-circle w-5 mr-3"></i>
                Quiz
            </a>

            <a href="{{ path('coach_mental_health_index') }}" class="sidebar-link {{ app.request.attributes.get('_route') == 'coach_mental_health_index' ? 'active' : '' }}">
                <i class="fas fa-brain w-5 mr-3"></i>
                Mental Health
            </a>

            <div class="space-y-1">
                <a href="{{ path('coach_recette') }}" class="sidebar-link {{ app.request.attributes.get('_route') == 'coach_recette' ? 'active' : '' }}">
                    <i class="fas fa-utensils w-5 mr-3"></i>
                    Nutrition
                </a>
                <a href="{{ path('coach_nutrition_consumption') }}" class="sidebar-link ml-6 text-sm {{ app.request.attributes.get('_route') == 'coach_nutrition_consumption' ? 'active' : '' }}" style="padding-top: 0.5rem; padding-bottom: 0.5rem; opacity: 0.8;">
                    <i class="fas fa-history w-4 mr-2"></i>
                    Meal Logs
                </a>
            </div>
        </nav>

        <!-- Logout -->
        <div class="p-3 border-t border-gray-700 mb-32">
            <a href="{{ path('app_logout') }}" class="sidebar-link logout-link hover:bg-red-500/10 hover:text-red-400">
                <i class="fas fa-sign-out-alt w-5 mr-3"></i>
                Logout
            </a>
        </div>
    </div>
</aside>

<style>
    @keyframes float {
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-10px); }
    }
    
    .animate-float {
        animation: float 3s ease-in-out infinite;
    }
</style>
