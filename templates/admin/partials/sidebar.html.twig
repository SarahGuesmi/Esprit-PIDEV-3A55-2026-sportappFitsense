<aside class="sidebar fixed inset-y-0 left-0 z-40 w-64 transform transition-transform duration-300 lg:translate-x-0 -translate-x-full">
    <div class="h-full flex flex-col">
        <!-- FitSense Header with Logo -->
        <div class="p-6 pb-4 border-b border-gray-700">
            <div class="text-center">
                <h1 class="text-2xl font-bold mb-3">
                    <span class="bg-gradient-to-r from-blue-400 to-purple-400 bg-clip-text text-transparent">
                        FitSense
                    </span>
                </h1>
                <img src="/assets/images/sport-hero.png" alt="FitSense" class="mx-auto w-28 mb-2 animate-float">
                <p class="text-gray-400 text-xs font-medium uppercase tracking-wide">Admin Panel</p>
            </div>
        </div>

        <!-- Navigation principale -->
        <nav class="flex-1 px-3 py-6 space-y-1 overflow-y-auto">
            <a href="{{ path('admin_dashboard') }}" class="sidebar-link {{ app.request.attributes.get('_route') == 'admin_dashboard' ? 'active' : '' }}">
                <i class="fas fa-tachometer-alt w-5 mr-3"></i>
                Dashboard
            </a>

            <a href="{{ path('admin_users_index') }}" class="sidebar-link {{ app.request.attributes.get('_route') starts with 'admin_users' ? 'active' : '' }}">
                <i class="fas fa-users w-5 mr-3"></i>
                Users
            </a>

            <a href="#" class="sidebar-link">
                <i class="fas fa-table w-5 mr-3"></i>
                Tables
            </a>

            <a href="#" class="sidebar-link">
                <i class="fas fa-dumbbell w-5 mr-3"></i>
                Exercises
            </a>

            <a href="#" class="sidebar-link">
                <i class="fas fa-calendar-alt w-5 mr-3"></i>
                Sessions
            </a>

            <a href="#" class="sidebar-link">
                <i class="fas fa-chart-bar w-5 mr-3"></i>
                Analytics
            </a>
        </nav>

        <!-- Section AUTH PAGES -->
        <div class="px-3 py-6 border-t border-gray-700">
            <p class="px-3 text-xs font-semibold text-gray-400 uppercase tracking-wide mb-3">AUTH PAGES</p>
            <a href="{{ path('auth_sign_in') }}" class="sidebar-link">
                <i class="fas fa-sign-in-alt w-5 mr-3"></i>
                Sign In
            </a>
            <a href="{{ path('auth_sign_up') }}" class="sidebar-link">
                <i class="fas fa-user-plus w-5 mr-3"></i>
                Sign Up
            </a>
        </div>

        <!-- Documentation -->
        <div class="px-3 py-3 border-t border-gray-700">
            <a href="#" class="sidebar-link">
                <i class="fas fa-book w-5 mr-3"></i>
                Documentation
            </a>
        </div>

        <!-- Logout -->
        <div class="p-3 border-t border-gray-700 mt-auto">
            <a href="#" class="sidebar-link logout-link hover:bg-red-500/10 hover:text-red-400">
                <i class="fas fa-sign-out-alt w-5 mr-3"></i>
                Logout
            </a>
        </div>
    </div>
</aside>

<style>
    @keyframes float {
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-10px); }
    }
    
    .animate-float {
        animation: float 3s ease-in-out infinite;
    }
</style>
